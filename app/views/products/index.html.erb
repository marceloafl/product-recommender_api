<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
    <h2>Produto: <%= product[:name] %></h2>
    <p>Categoria: <%= product[:category] %></p>
    <p>Preferências:</p>
    <ul>
        <% product[:preferences].each do |preference| %>
          <li><%= preference %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>
